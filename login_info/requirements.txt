# ============================================================
# 核心依赖 —「获取当前登录用户信息」接口
# ============================================================

# Web 框架：FastAPI（现代化、高性能、原生支持 Pydantic 类型校验）
fastapi==0.115.6

# ASGI 服务器：用于本地开发和部署
uvicorn[standard]==0.34.0

# JWT 令牌生成与校验
PyJWT==2.10.1

# 密码哈希（用户认证相关场景复用）
passlib[bcrypt]==1.7.4

# 数据校验：FastAPI 已内置，此处显式声明以便 IDE 提供更好的提示
pydantic==2.10.4
