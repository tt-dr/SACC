Alembic migration revisions are generated in this directory.

The initial deployment can use `database-scripts/001_init.sql`. After the schema
is under active development, generate and review Alembic revisions for changes.

